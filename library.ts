import { Book } from "./book";

 export class library{
    private books:Book[] = [];
    
    addbook(book :Book){
        this.books.push(book);
    }
    listbooks(){
        this.books.forEach(book_ => {
            book_.bookdetails();
        });

        }

        removeBook(id: number): void {
            this.books = this.books.filter(book => book.book_id !== id);
        }
    
}













































































































































































































































































































